import type { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'io.gilbert.mydigitalwallet',
  appName: 'MyDigitalWallet',
  webDir: 'www'
};

export default config;
